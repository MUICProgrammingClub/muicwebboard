<nav class="navbar navbar-default navbar-static-top">
<div class= "container">

	<div id="navbar" class="navbar-collapse collapse">
		<ul class="nav navbar-nav" >
			<li class="dropdown"> <a href="#" class="dropdown-toggle" data-toggle="dropdown" role="button" aria-haspopup="true" aria-expanded="false">
			<span class="glyphicon glyphicon-menu-hamburger"></span></a>
				<ul class="dropdown-menu">
					<li><%= link_to 'Home', root_path %></li>
					<li><%= link_to 'Lectures', lectures_path %></li>
					<li><%= link_to 'Courses', courses_path %></li>
					<li><%= link_to 'Instructor', instructors_path %></li>
					<% if can? :update, @lectures %>
					<li><%= link_to 'User Management', users_path %></li>
					<%end%>

				</ul>
			</li>
			<% if request.path != root_path && request.path != static_pages_path %>
				<a class="navbar-brand" href=<%= root_path %>> IC NETWORK </a>
			<% end %>
		</ul>

		<ul class="nav navbar-nav navbar-right">
		<% if user_signed_in? %>
			<li class="dropdown">
              	<a href="#" class="dropdown-toggle" data-toggle="dropdown" role="button" aria-haspopup="true" aria-expanded="false">
              	<%= image_tag current_user.avatar.url(:thumb), width:'20px', height: '30px' %>
              	<%= current_user.first_name %>
              <span class="caret"></span></a>
              <ul class="dropdown-menu">
                <li><%= link_to '<span class="glyphicon glyphicon-edit"></span> Edit User Profile'.html_safe, edit_user_registration_path %></li>
                <li><%= link_to '<span class="glyphicon glyphicon-user"></span> Your Profile'.html_safe, user_path(current_user.id) %></li>
                <li><%= link_to '<span class="glyphicon glyphicon-log-out"></span> Sign Out'.html_safe, destroy_user_session_path, :method => :delete, :flash => {:notice => "You have successfully logout"} %>
              </ul>
            </li>
		<% else %>
			<li> <a href=<%= user_session_path %>> Sign In </a> </li>
		<% end %>
		</ul>
	</div>
</div>
</nav>
